# /etc/dcentrald-experimental.toml - Antminer S21 XP evidence target
#
# SHIPPED INERT. This file contains no executable ASIC opt-in. The reusable
# BM1370 driver identity (0x1370 / 4976) does not make the S21 XP Amlogic
# platform executable.
#
# Exact production evidence maps the three chains to ttyS3/ttyS2/ttyS1 and
# contradicts the shared base-S21 ttyS4/NoPic composition. Production PIC and
# PSU selection, electrical safe-off, cooling custody, lifecycle unwind, work
# ownership, accepted shares, storage geometry, install, and recovery remain
# unresolved.
#
# BoardDesc is management-only and install-denied, the daemon model is
# EvidenceOnly/TD003-gated, the Amlogic HAL explicitly refuses S21 XP, and the
# rootfs mutation policy is management-only. Editing this file cannot override
# any of those independent gates.
